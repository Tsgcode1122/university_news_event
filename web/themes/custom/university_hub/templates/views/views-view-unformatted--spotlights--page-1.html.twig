{% if title %}<h2>{{ title }}</h2>{% endif %}
<div class="news-listing-grid spotlights-listing-grid">
  {% for row in rows %}
    <div{{ row.attributes.addClass('news-listing-item') }}>{{ row.content }}</div>
  {% endfor %}
</div>
